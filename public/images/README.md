# Images

Add site imagery here as real assets become available.

- `fence-boundary.png` — fence line / forest boundary at dusk, used in the Home page's
  Challenge section (`src/components/home/Challenge.jsx`). Recommended: 800x600px or larger,
  landscape orientation, moody/dark tone.

The Challenge section currently renders a styled placeholder box (no broken image icon) until
this file is dropped in and the component is wired up to use it.
